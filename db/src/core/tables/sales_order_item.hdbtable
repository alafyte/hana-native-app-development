COLUMN TABLE "com.sap.training.sales.core::sales_order_item" (
  ITEM_ID BIGINT NOT NULL,
  ORDER_ID BIGINT NOT NULL,
  PRODUCT_ID BIGINT NOT NULL,
  QUANTITY INTEGER,
  UNIT_PRICE DECIMAL(15, 2),
  DISCOUNT DECIMAL(5, 2),
  PRIMARY KEY (ITEM_ID)
)
